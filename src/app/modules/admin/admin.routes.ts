import { Routes } from '@angular/router';
import { AdminFormComponent } from './components/admin-form/admin-form.component';

export const routes: Routes = [{ path: '', component: AdminFormComponent }];
